// node understands that this file is js already.
const logger = require('./logger');
// to load the module we use the require function


logger.log('message');

//console.log(logger);

// if the project is not the same folder we can put the path ('./subFolder/fileName') or ('../parentFolder/fileName')
