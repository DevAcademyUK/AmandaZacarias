/*
import java.util.Comparator;

class Login implements Comparable<Employee> {
    //Declaring variables.
    public String userName;
    public String password;

    //Constructor of the class Login.
    public Login(String user, String pass) {
        this.userName = user;
        this.password = pass;
    }





}

/*    @Override
    public int compareTo (Login o) {
        String user = o.userName;
        return this.userName.compareTo(user);
    }

    public static Comparator<Employee> userNameComparator = (u1, u2) -> {

        String user1 = u1.userName.toUpperCase();
        String user2 = u2.userName.toUpperCase();

        return user1.compareTo(user2);
    };

    public static Comparator<Login> passwordComparator = (p1, p2) -> {
        String password1 = p1.password;
        String password2 = p2.password;

        return password1.compareTo(password2);
    };
}
*/